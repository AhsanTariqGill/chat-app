<h1>Users Present</h1>

<div class="box">
  <ul>
    <% @users.each do |user| %>
      <li>
        <%current = "(You)" if current_user?(user)%>
        <%= link_to "#{user.name.capitalize()} #{current}", admin_user_path(user)%>
      </li>
    <% end %>
  </ul>
</div>
